import type { Metadata } from "next"
import { Content } from "./content"

export const metadata: Metadata = {
  title: "Roadmap | Where the Citationly Platform Is Headed",
  description:
    "The direction of the Citationly AI search platform: what we are building now, what comes next, and what we are exploring, shaped by customer feedback.",
}

export default function RoadmapPage() {
  return <Content />
}
